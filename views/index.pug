extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 My Portfolio

      p.lead.text-light
        | This portfolio tells a story. It's an amazing journey throughout all the projects I worked on and that had allowed me to develop my skills as a Full Stack developer. 
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in projects
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=project.image_urls[1])
            h5 #{project.project_name}
      
